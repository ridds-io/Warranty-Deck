# Core dependencies for receipt OCR with docTR
pydantic==2.6.4
pillow==10.4.0
opencv-python-headless==4.10.0.84
pdf2image==1.16.3
python-doctr==0.8.1
python-dotenv==1.0.1
numpy==1.26.4
regex==2024.4.16

# API Server
flask==3.0.0
flask-cors==4.0.0

# Testing
pytest==8.2.1

# Note: Install PyTorch CPU wheels first:
# pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cpu
